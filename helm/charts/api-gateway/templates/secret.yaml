apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secret
  labels:
    app: {{ .Chart.Name }}
type: Opaque
stringData:
  JWT_SECRET: {{ if and .Values.global .Values.global.jwtSecret }}{{ .Values.global.jwtSecret | quote }}{{ else }}"super-secret-change-me"{{ end }}
