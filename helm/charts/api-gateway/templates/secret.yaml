apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secret
  labels:
    app: {{ .Chart.Name }}
type: Opaque
stringData:
  JWT_SECRET: {{ dig "global" "jwtSecret" "super-secret-change-me" .Values | quote }}
